@model BaseModel
@inherits StatiqRazorPage<BaseModel>

<footer class="bg-black">
  <div class="container max-w-6xl mx-auto flex items-center px-2 py-8">

    <div class="w-full mx-auto flex flex-wrap items-center">
      <div class="flex w-full md:w-1/2 justify-center md:justify-start text-white">
        <a class="text-gray-400 no-underline hover:text-white duration-500 transition ease-in-out hover:no-underline mr-2" href="#">
          <span class="text-sm">The Freeze Team</span>
        </a>
        <a class="text-gray-400 no-underline hover:text-white duration-500 transition ease-in-out hover:no-underline" href="#">
          <span class="text-sm">&copy; @DateTime.Now.Year</span>
        </a>
      </div>
      <div class="flex w-full pt-2 content-center justify-between md:w-1/2 md:justify-end">
        <ul class="list-reset flex justify-center flex-1 md:flex-none items-center">
          @if (Model.RssFeeds != null)
          {
            <li>
              <a
                class="text-sm inline-block text-gray-400 no-underline hover:text-gray-200 hover:text-gray-300 transition duration-500 ease-in-out py-2 px-3"
                href="@Context.GetLink(Model.RssFeeds.FirstOrDefault())"
              >
                RSS Feed
              </a>
            </li>
          }
          @if (Model.AtomFeeds != null)
          {
            <li>
              <a
                class="text-sm inline-block text-gray-400 no-underline hover:text-gray-200 hover:text-gray-300 transition duration-500 ease-in-out py-2 px-3"
                href="@Context.GetLink(Model.AtomFeeds.FirstOrDefault())"
              >
                Atom Feed
              </a>
            </li>
          }
        </ul>
      </div>
    </div>
  </div>
</footer>

