@{ 
    bool isPostType = Document.GetBool("IsPost");
}
    @*<div class="container">
            <div class="row">
                    <div class="col-md-12 text-center">
                        <p class="copyright text-muted m-0">Copyright © @DateTime.Now.Year</p>
                        <br />
                        <ul class="list-inline text-center small">
                            @{
                                IDocument rssFeed = Outputs["**/*.rss"].FirstOrDefault();
                                IDocument atomFeed = Outputs["**/*.atom"].FirstOrDefault();
                            }
                            @if (rssFeed != null) {
                                <li class="list-inline-item">
                                    <a href="@Context.GetLink(rssFeed)"><i class="fa fa-rss"></i> RSS Feed</a>
                                </li>
                            }
                            @if (atomFeed != null) {
                                <li class="list-inline-item">
                                    <a href="@Context.GetLink(atomFeed)"><i class="fa fa-rss"></i> Atom Feed</a>
                                </li>
                            }
                        </ul>
                        <br />
                        <div class="font-weight-bold small"><a href="https://statiq.dev">Generated by Statiq</a></div>
                    </div>
            </div>
        </div>*@
    <!--Subscribe-->
    @if (isPostType)
    {
        <!--Author-->
        <div class="flex w-full items-center font-sans p-8 md:p-24">
            <div class="flex-1">
                <p class="text-base font-bold text-base md:text-xl leading-none">
                   Author: @Document.GetString(WebKeys.Author)
                </p>
            </div>
            <div class="justify-end">
                <button class="bg-transparent border border-gray-500 hover:border-green-500 text-xs text-gray-500 hover:text-green-500 font-bold py-2 px-4 rounded-full transition duration-500 ease-in-out">Read More</button>
            </div>
        </div>
    <!--/Author-->
    }
    else
    {
        <div class="container font-sans bg-green-100 rounded mb-8 mt-8 p-4 md:p-24 text-center">
            <h2 class="font-bold break-normal text-2xl md:text-4xl">Subscribe to TimeWarp Enterprise</h2>
            <h3 class="font-bold break-normal font-normal text-gray-600 text-base md:text-xl">Get the latest article about C# to your box.</h3>
            <div class="w-full text-center pt-4">
                <form action="#">
                    <div class="max-w-xl mx-auto p-1 pr-0 flex flex-wrap items-center">
                        <input type="email" placeholder="youremail@example.com" class="flex-1 appearance-none rounded shadow p-3 text-gray-600 mr-2 focus:outline-none">
                        <button type="submit" class="transition duration-500 ease-in-out flex-1 mt-4 md:mt-0 block md:inline-block appearance-none bg-green-500 text-white text-base font-semibold tracking-wider uppercase py-4 rounded shadow hover:bg-green-400">Subscribe</button>
                    </div>
                </form>
            </div>
        </div>
    <!-- /Subscribe-->
    }

